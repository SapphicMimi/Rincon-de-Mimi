export interface DataState {
  loading: boolean;
  error: boolean;
  errorMessage: string;
}

export interface userData {
  direccion: string;
  codPostal: string;
  ciudad: string
  provincia: string;
}
